# Copie para cada módulo back: cp infra/.env.example academico/back/.env
# (ou use npm start — o script infra/start.js cria .env a partir de .env.example)

DATABASE_URL=postgresql://uniportal:uniportal@localhost:5432/uniportal
DATABASE_SSL=false
